services:
  caddy:
    image: caddybuilds/caddy-cloudflare:alpine
    container_name: caddy_homelab
    pull_policy: always
    network_mode: host
    environment:
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN}
      - REVERSE_PROXY_URL=${REVERSE_PROXY_URL}
      - JELLYFIN=${JELLYFIN}
      - JELLYFIN_PORT=${JELLYFIN_PORT}
      - JELLYFIN_REVERSE_PROXY_PORT=${JELLYFIN_REVERSE_PROXY_PORT}
      - DAWARICH=${DAWARICH}
      - DAWARICH_PORT=${DAWARICH_PORT}
      - DAWARICH_REVERSE_PROXY_PORT=${DAWARICH_REVERSE_PROXY_PORT}
    env_file:
      - .env
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./Caddyfile.template:/etc/caddy/Caddyfile.template
      - ./caddy_data:/data
      - ./caddy_config:/config
      - ./start_caddy.sh:/start_caddy.sh
    ports:
      #TODO open the ports in server too
      - 8920:8920
      - 8082:8082
      - 8086:8086
    restart: unless-stopped
    entrypoint: ["/bin/sh", "/start_caddy.sh"]
  beszel-agent:
    image: henrygd/beszel-agent
    container_name: beszel-agent-apps.prinzpiuz.in
    restart: unless-stopped
    pull_policy: always
    network_mode: host
    volumes:
      - ./configs/beszel_agent_data:/var/lib/beszel-agent
      - ./configs/beszel_socket:/beszel_socket
      - /var/run/docker.sock:/var/run/docker.sock:ro
    env_file:
      - .env
    cap_add:
      - SYS_RAWIO
      - SYS_ADMIN
      - CAP_PERFMON
    devices:
      - /dev/sda:/dev/sda
    environment:
      - LISTEN=/beszel_socket/beszel.sock
      - HUB_URL=http://${HOMELAB_TAILNET_IP}:8090
      - TOKEN=${BESZEL_TOKEN}
      - KEY=${BESZEL_KEY}
