{
    http_port 8082
}

${JELLYFIN}:${JELLYFIN_PORT} {
    reverse_proxy ${REVERSE_PROXY_URL}:${JELLYFIN_REVERSE_PROXY_PORT}
    tls {
        dns cloudflare ${CLOUDFLARE_API_TOKEN}
    }
}


${DAWARICH}:${DAWARICH_PORT} {
    reverse_proxy ${REVERSE_PROXY_URL}:${DAWARICH_REVERSE_PROXY_PORT}
    tls {
        dns cloudflare ${CLOUDFLARE_API_TOKEN}
    }
}
